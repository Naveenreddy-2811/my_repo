name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on:  ubuntu-latest
    steps:
      -  name:  Welcome to Github actions
         run:  echo "my first job"
      -  name:  Second step
         run: echo "My second jobs"
